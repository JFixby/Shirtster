# Shirtster

Small side project, a platformer using the LibGDX framework and the Tiled Map Editor.

To run:
```
git clone https://github.com/StamplerJ/Shirtster.git shirtster
cd shirtster
./gradlew desktop:run
```
